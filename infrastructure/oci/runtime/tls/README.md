# Runtime TLS Files

Place runtime certificate files under this directory for Terraform-managed HTTPS:

- `wortwerk.xyz/fullchain.pem`
- `wortwerk.xyz/privkey.pem`

These files are intentionally gitignored and should not be committed.
